{
  "name": "erpzenith",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run setup --force && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.5"
  }
}
