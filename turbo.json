{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"],
      "env": ["FRONTEND_URL"]
    },
    "generate": {
      "cache": false
    },
    "setup": {
      "dependsOn": ["generate"],
      "cache": false
    },
    "dev": {
      "dependsOn": ["setup"],
      "cache": false,
      "persistent": true
    },
    "lint": {}
  }
}
