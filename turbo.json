{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"],
      "env": ["FRONTEND_URL", "NEXT_PUBLIC_API_URL", "BACKEND_URL", "NODE_ENV"]
    },
    "generate": {
      "cache": false
    },
    "setup": {
      "dependsOn": ["generate"],
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {}
  }
}
